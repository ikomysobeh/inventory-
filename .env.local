# Dev overrides — do NOT set VITE_API_URL here unless you need to
# point at a specific host (e.g. a staging server).
# Leaving it unset lets api.ts derive the URL from window.location.origin,
# which works correctly on any device (laptop, phone, etc.).
